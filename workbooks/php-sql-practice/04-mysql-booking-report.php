<?php

// You're building features for Sherpa Hospitality Group.
// Given these tables, write a query that returns:
// "All guests who have checked in during July 2026,
//  sorted by checkout date (soonest first),
//  showing: guest name, room number, checkout date."

// Tables:
//   guests: id, name, email, phone
//   reservations: id, guest_id, room_id, checkin, checkout, status
//   rooms: id, room_number, room_type, rate_per_night

// Write the SQL query below as a string:

$query = "YOUR SQL HERE";

echo $query;
