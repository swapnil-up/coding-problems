<script>
  // TODO: declare all state variables:
  // let name = $state('');
  // let age = $state(0);
  // let plan = $state('free');
  // let newsletter = $state(false);
  // let interests = $state([]);
  // let contactMethod = $state('email');
</script>

<!-- TODO: text input bound to name, label "Name" -->

<!-- TODO: number input bound to age, label "Age" -->

<!-- TODO: select bound to plan with options free/pro/enterprise, label "Plan" -->

<!-- TODO: checkbox bound to newsletter, label "Newsletter" -->

<!-- TODO: group of checkboxes bound to interests with values coding/design/writing -->

<!-- TODO: radio buttons bound to contactMethod with values email/phone/none -->

<!-- TODO: preview div with data-testid="preview" showing all current values -->
<!-- Format: name=Alice age=25 plan=pro newsletter=true interests=coding,design contact=email -->
