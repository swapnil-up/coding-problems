## Hints

- `<svelte:head><title>My Title</title></svelte:head>` sets the browser tab title
- SvelteKit links are just plain `<a href="/path">` — no special component needed
- The `src/routes/about/+page.svelte` file automatically maps to the `/about` URL
