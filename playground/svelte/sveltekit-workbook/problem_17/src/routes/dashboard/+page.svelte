<h1>Dashboard (default: ssr=true, csr=true)</h1>
<p>This page uses SvelteKit's default behavior:</p>
<ul>
  <li>SSR: rendered on the server first (fast initial load, SEO-friendly)</li>
  <li>CSR: hydrated on the client (interactive after load)</li>
  <li>No prerendering: fresh data on every request</li>
</ul>
